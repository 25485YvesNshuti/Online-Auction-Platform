# Disable JPA open-in-view warning
spring.jpa.open-in-view=false
spring.application.name=Online-Auction-Platform

# Database Configuration (uses environment variable from Render)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/online_auction_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Nshutiyves@2023}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# Server port (Render provides this)
server.port=${PORT:8080}

# JWT Configuration
jwt.secret=${JWT_SECRET:BT9TwCrsB0//sma+0qu/jGlmfQOmkpyciJX9P9vqoJX3f4N/u0oLRLbUREiFMC5mEJLbM2dm1AqrwMwvCuk2qQ==}
jwt.expiration=86400000

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:nshutiyves10.info@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:ufnbsyajmpumahlq}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Logging
logging.level.com.petshop.onlinepetshop=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.root=INFO

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB